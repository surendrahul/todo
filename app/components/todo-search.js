import Component from '@ember/component';

export default Component.extend({
    // classNames:['todo-search'],
    // value:'',
    // init(){
    //     //console.log('this is init method in todo search')
    //     this._super(...arguments)
    //     this.filter('').then((results) => this.set('results',results));

    // },
    // actions:{
    //     searchTask(){
    //         console.log('this is search task in action in todo search')
    //         let searchInputValue=this.value;
    //         let filterAction=this.filter;
    //         console.log("search Input value ",searchInputValue)
    //         console.log("this is filter action ",filterAction)
    //         filterAction(searchInputValue).then((filterResults) => this.set('results', filterResults));

    //     }
    // }
});
